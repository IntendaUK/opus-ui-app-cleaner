# All run artifacts (reports, backups, trash, baseline) — cleared by the next
# --apply run (or npm run revert). output/
*.logdeleted-files and output/*-backup
# are the undo record for the most recent run.
output/

# Machine-specific entrypoints (copy entrypoints.sample.txt)
entrypoints.txt

node_modules/
